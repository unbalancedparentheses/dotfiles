# Secrets configuration - COPY THIS TO secrets.nix AND FILL IN VALUES
# DO NOT commit secrets.nix to git (it's in .gitignore)
{
  # API Keys
  github_token = "";
  openai_api_key = "";
  anthropic_api_key = "";

  # SSH
  ssh_key_path = "~/.ssh/id_ed25519";

  # Git signing
  git_signing_key = "";

  # Cloud providers
  aws_access_key_id = "";
  aws_secret_access_key = "";
  digitalocean_token = "";

  # Database URLs
  database_url = "";

  # Other secrets
  # Add your own here...
}
